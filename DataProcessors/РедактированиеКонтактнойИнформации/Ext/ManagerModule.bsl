#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
////////////////////////////////////////////////////////////////////////////////
// ПРОГРАММНЫЙ ИНТЕРФЕЙС

// Процедура заполняет запись данными из реквизитов обработки.
//
// Параметры:
//  Запись - Менеджер записи или запись из набора
//
Процедура ЗаполнитьЗапись(Запись, Объект) Экспорт

	Запись.Объект        = Объект.Объект;
	Запись.Тип           = Объект.Тип;
	Запись.Вид           = Объект.Вид;
	Запись.Представление = Объект.Представление;
	Запись.Комментарий   = Объект.Комментарий;
	Для а = 1 По 10 Цикл
		Запись["Поле" + Строка(а)] = Объект["Поле" + Строка(а)];
	КонецЦикла;

КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ

////////////////////////////////////////////////////////////////////////////////
// СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ

#КонецЕсли
