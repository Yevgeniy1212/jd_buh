
#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ВыполнитьОбработку(Команда) 
	
	// Для запуска выполнения обработчика, параметр, 
	// возвращаемый данной формой, должен быть структурой.
	ПараметрыОбработки = Новый Структура;
	ВыполнитьОбработкуНаСервере();
	ЭтаФорма.Закрыть(ПараметрыОбработки);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ВыполнитьОбработкуНаСервере()
	//Обработки.си_ОбновлениеИнформационнойБазы.ПроверкаОбновления20113();
КонецПроцедуры

#КонецОбласти