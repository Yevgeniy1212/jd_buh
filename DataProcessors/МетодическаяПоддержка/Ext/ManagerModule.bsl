#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

Функция ПолучитьТаблицуИнформационныхСсылокДляВиджета() Экспорт  
	
	ЛокализованноеИмяМакета = ОбщегоНазначенияБК.ПолучитьИмяМакетаНаТекущемЯзыкеПользователя("Обработка.МетодическаяПоддержка", "ИнформационныеСсылки");
	
	Если НЕ ЗначениеЗаполнено(ЛокализованноеИмяМакета) Тогда 
		Возврат Новый ТаблицаЗначений;
	КонецЕсли;
		
	Макет = Обработки.МетодическаяПоддержка.ПолучитьМакет(ЛокализованноеИмяМакета); 
	Макет.КодЯзыкаМакета = ТекущийЯзык().КодЯзыка; 
	
	ИнформационныеСсылкиXML = Макет.ПолучитьТекст();
	ТаблицаСсылок = ОбщегоНазначения.ПрочитатьXMLВТаблицу(ИнформационныеСсылкиXML).Данные;
	Если ПравоДоступа("Просмотр", Метаданные.ПланыСчетов.Типовой) Тогда
		РольПользователя = "Бухгалтер";
	Иначе
		РольПользователя = "Руководитель";
	КонецЕсли;
	
	Строки = ТаблицаСсылок.НайтиСтроки(Новый Структура("Роль", РольПользователя));
	ТаблицаРезультата = ТаблицаСсылок.Скопировать(Строки);
	ТаблицаРезультата.Сортировать("Вес");
	
	Возврат ТаблицаРезультата;
	
КонецФункции

Функция ПолучитьИмяСобытияДляЖурналаРегистрации() Экспорт
	
	Возврат НСтр("ru = 'Методическая поддержка'", ОбщегоНазначенияКлиентСервер.КодОсновногоЯзыка());
	
КонецФункции

#КонецОбласти

#КонецЕсли