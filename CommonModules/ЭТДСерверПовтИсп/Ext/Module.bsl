
#Область ПрограммныйИнтерфейс

// Возвращает параметры обмена с сервисом ЭТД: Адреса веб-страниц, логины и пароли.
// 
// Возвращаемое значение:
//  Структура - Актуальные параметры обмена
//
Функция ПолучитьПараметрыЭТД() Экспорт
	
	ПараметрыЭТД = Константы.ПараметрыЭТД.Получить().Получить();
	Если ПараметрыЭТД = Неопределено Тогда
		ПараметрыЭТД = Новый Структура;
		ПараметрыЭТД.Вставить("АдресСервисаЭТД", ЭТДКлиентСервер.АдресПродуктивногоСтенда());
		ПараметрыЭТД.Вставить("ИспользоватьВнешнююКриптографиюДляКомпоненты", Ложь);
	КонецЕсли;
	
	Возврат ПараметрыЭТД;
	
КонецФункции

#Область Криптобиблиотека

Функция ВыполнятьКриптографическиеОперацииНаКлиенте() Экспорт
	
	Если Константы.МестоВыполненияКриптографическихОпераций.Получить() = Перечисления.МестаВыполненияКриптографическихОпераций.НаКлиенте Тогда
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#КонецОбласти
