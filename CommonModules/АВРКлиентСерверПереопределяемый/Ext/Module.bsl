
#Область ОтражениеАктовВУчете
	
Функция ПолучитьСписокДокументовОснованияДляАВР(Направление = Неопределено) Экспорт
	
	// Получить список документов ЭДВС по типу формы.
	ДокументыВыбора = Новый СписокЗначений;
	
	//БК3
	Если Направление = ПредопределенноеЗначение("Перечисление.НаправленияЭСФ.Входящий") Тогда	
		// Список документов основания - входящего АВР
		ДокументыВыбора.Добавить("ПоступлениеТоваровУслуг", НСтр("ru='Поступление ТМЗ и услуг'"));
		ДокументыВыбора.Добавить("ПоступлениеДопРасходов", НСтр("ru='Поступление доп. расходов'"));
		
	Иначе
		// Список документов основания - исходящего АВР
		ДокументыВыбора.Добавить("РеализацияТоваровУслуг", НСтр("ru='Реализация ТМЗ и услуг'"));
		ДокументыВыбора.Добавить("АктОбОказанииПроизводственныхУслуг", НСтр("ru='Акт об оказании производственных услуг'"));
		ДокументыВыбора.Добавить("РеализацияУслугПоПереработке", НСтр("ru='Реализация услуг по переработке'"));
	КонецЕсли;
	
	//ЕРП - прототип
	//Если Направление = ПредопределенноеЗначение("Перечисление.НаправленияЭСФ.Входящий") Тогда	
	//	// Список документов основания - входящего АВР
	//	ДокументыВыбора.Добавить("ПриобретениеТоваровУслуг", "Приобретение товаров и услуг");
	//	ДокументыВыбора.Добавить("ПриобретениеУслугПрочихАктивов", "Приобретение услуг и прочих активов");
	//Иначе
	//	// Список документов основания - исходящего АВР
	//	ДокументыВыбора.Добавить("АктВыполненныхРабот", "Акт выполненных работ для клиента");
	//КонецЕсли;
	
	Возврат ДокументыВыбора;
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// См. АВРКлиентСервер.КонтейнерМетодов().
Процедура КонтейнерМетодов(Контейнер, ИспользоватьВнешнююОбработку) Экспорт
	
	
	
КонецПроцедуры

Процедура УстановитьЭлементОтбораДинамическогоСписка(ДинамическийСписок, ИмяПоля,
	ПравоеЗначение = Неопределено,
	ВидСравнения = Неопределено,
	Представление = Неопределено,
	Использование = Неопределено,
	РежимОтображения = Неопределено,
	ИдентификаторПользовательскойНастройки = Неопределено) Экспорт
	
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(ДинамическийСписок, ИмяПоля,
		ПравоеЗначение,
		ВидСравнения,
		Представление,
		Использование,
		РежимОтображения,
		ИдентификаторПользовательскойНастройки);
	
КонецПроцедуры

//Определяет можно ли создавать ЭАВР на основании непроведенных первичных документов
//
Функция КонтрольПроведенияДокументаОснования() Экспорт
	
	Возврат Ложь;
	
КонецФункции

//Определяет можно ли создавать исправленные ЭАВР на группу первичных документов
//
Функция ДоступноГрупповоеСозданиеИсправленных() Экспорт
	
	Возврат Ложь;
	
КонецФункции

#КонецОбласти
