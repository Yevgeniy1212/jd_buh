
#Область ПрограммныйИнтерфейс

Процедура УдалитьСтрокиТаблицыНомераГТД(КлючСвязи, ТаблицаНомераГТД) Экспорт
	
	Пока Истина Цикл
		
		СтрокаУдалена = Ложь;
		
		Для Каждого СтрокаТаблицыНомераГТД Из ТаблицаНомераГТД Цикл
			Если СтрокаТаблицыНомераГТД.КлючСвязи = КлючСвязи Тогда
				ТаблицаНомераГТД.Удалить(СтрокаТаблицыНомераГТД);
				СтрокаУдалена = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если НЕ СтрокаУдалена Тогда
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция ПредставленияПризнаковПроисхождения() Экспорт
	
	ПредставленияПризнаков = Новый Массив();
	ПредставленияПризнаков.Добавить(НСтр("ru='1 - Товар, включенный в Перечень, ввезенный на территорию РК'"));
	ПредставленияПризнаков.Добавить(НСтр("ru='2 - Товар, не включенный в Перечень, ввезенный на территорию РК'") );
	ПредставленияПризнаков.Добавить(НСтр("ru='3 - Товар, включенный в Перечень, произведенный на территории РК'") );
	ПредставленияПризнаков.Добавить(НСтр("ru='4 - Товар, не включенный в Перечень, произведенный на территории РК'") );
	ПредставленияПризнаков.Добавить(НСтр("ru='5 - Товар, не относящийся к другим признакам'") );
	
	Возврат ПредставленияПризнаков;	
	
КонецФункции

#КонецОбласти

#Область СтроковыеКонстанты

Функция СпособЗаполнения_СначалаПустыми() Экспорт
	Возврат "СначалаПустыми";	
КонецФункции

Функция СпособЗаполнения_СначалаЗаполненными() Экспорт
	Возврат "СначалаЗаполненными";	
КонецФункции

#КонецОбласти
