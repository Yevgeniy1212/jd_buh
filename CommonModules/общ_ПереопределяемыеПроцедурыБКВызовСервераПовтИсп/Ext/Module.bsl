
#Область ПрограммныйИнтерфейс

// Функция возвращает право печати непроведенных документов.
//
// Параметры:
//  нет.
//
// Возвращаемое значение:
//  Истина - если можно печатать, иначе Ложь.
//
Функция РазрешитьПечатьНепроведенныхДокументов() Экспорт
	
	Возврат общ_ПереопределяемыеПроцедурыБКСервер.РазрешитьПечатьНепроведенныхДокументов(); 
	
КонецФункции

// Преобразует согласно заданых параметров счет БУ в счет НУ
//
// Параметры:
//  СтруктураДанных	 - Структура 
//  Тип				 - Строка 
//  ССубконто		 - Булево 
//  ДатаСреза		 - Дата 
// 
// Возвращаемое значение:
//   Структура - если если субконто, ПланыСчетов.Налоговый - в противном случае 
//
Функция ПреобразоватьСчетаБУвСчетНУ(СтруктураДанных, Тип = "НУ", ССубконто = Ложь, Знач ДатаСреза = Неопределено) Экспорт
	
	Возврат ПроцедурыБухгалтерскогоУчетаВызовСервераПовтИсп.ПреобразоватьСчетаБУвСчетНУ(СтруктураДанных, Тип, ССубконто , ДатаСреза);
	
КонецФункции

// Функция возвращает налогоплательщика для переданной структурной единицы
//
// Параметры:
//	СтруктурнаяЕдиница - Организация/Подразделение организации
//	Организация - организация из шапки документа
//	РазделНУ - значение перечисления РазделыНалоговогоУчета
//
Функция ПолучитьНалогоплательщикаСтруктурнойЕдиницы(СтруктурнаяЕдиница, Организация, РазделНУ) Экспорт
	//Возврат  бсо_УправлениеПроведениемДокументовСерверПереопределяемый.ПолучитьНалогоплательщикаСтруктурнойЕдиницы(СтруктурнаяЕдиница, Организация, РазделНУ);
	Возврат  ПроцедурыНалоговогоУчетаВызовСервераПовтИсп.ПолучитьНалогоплательщикаСтруктурнойЕдиницы(СтруктурнаяЕдиница, Организация, РазделНУ);
КонецФункции

// Функция - Получить валюту регламентированного учета
// 
// Возвращаемое значение:
//    Значение переменной из списка 
//
Функция ПолучитьВалютуРегламентированногоУчета() Экспорт
	Возврат си_ОбщегоНазначенияВызовСервераПовтИсп.ЗначениеПеременной("ВалютаРегламентированногоУчета");	
КонецФункции

// Функция - Счет можно использовать в проводках
//
// Параметры:
//  Счет - ПланыСчетов.Налоговый 
// 
// Возвращаемое значение:
//   Булево - Истина, если счет можно исползовать в проводках, Ложь - в противном случае
//
Функция СчетМожноИспользоватьВПроводках(Счет) Экспорт
	Возврат НЕ Счет.ЗапретитьИспользоватьВПроводках;	
КонецФункции

// Функция - Получить значение по умолчанию
//
// Параметры:
//  Настройка	 - Строка 
// 
// Возвращаемое значение:
//   Строка - значение прользовательской настройки 
//
Функция ПолучитьЗначениеПоУмолчанию(Настройка) Экспорт
	ТекПользователь = Пользователи.ТекущийПользователь();
	Возврат ПользователиБКВызовСервераПовтИсп.ПолучитьЗначениеПоУмолчанию(ТекПользователь, Настройка);
КонецФункции

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Функция возвращает ставку НДС
//
// Параметры:
//  СтавкаНДС - СправочникСсылка.СтавкиНДС, элемент справочника, для которого необходимо получить
//  			числовое значение ставки
//
// Возвращаемое значение:
//	Числовое значение ставки НДС.
//
Функция ПолучитьСтавкуНДС(СтавкаНДС) Экспорт
	Возврат УчетНДСиАкцизаВызовСервераПовтИсп.ПолучитьСтавкуНДС(СтавкаНДС);	
КонецФункции

// Функция возвращает ставку Акциза
//
// Параметры:
//  СтавкаАкциза - СправочникСсылка.СтавкиАкциза, элемент справочника, для которого необходимо получить
//  			числовое значение ставки
//
// Возвращаемое значение:
//	Числовое значение ставки Акциза.
//
Функция ПолучитьСтавкуАкциза(СтавкаАкциза) Экспорт
	Возврат УчетНДСиАкцизаВызовСервераПовтИсп.ПолучитьСтавкуАкциза(СтавкаАкциза);
КонецФункции // ПолучитьСтавкуНДС()

Функция ПолучитьСписокСчетовУчетаТМЗ(ПрефиксУчета = "БУ") Экспорт
	Возврат ПроцедурыБухгалтерскогоУчетаВызовСервераПовтИсп.ПолучитьСписокСчетовУчетаТМЗ(ПрефиксУчета);
КонецФункции

#КонецОбласти

