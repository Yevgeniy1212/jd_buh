
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ТабличныйДокумент = ПечатьНаСервере(ПараметрКоманды);
	ЭСФКлиентПереопределяемый.ОткрытьФормуПечати(ТабличныйДокумент, ПараметрКоманды);
	
КонецПроцедуры

&НаСервере
Функция ПечатьНаСервере(МассивЭСФ)
	
	ОбработкаОбменЭСФ = ЭСФСерверПовтИсп.ОбработкаОбменЭСФ();
	ТабличныйДокумент = ОбработкаОбменЭСФ.ПечатьЭСФ(МассивЭСФ);
	
	Возврат ТабличныйДокумент;
	
КонецФункции

