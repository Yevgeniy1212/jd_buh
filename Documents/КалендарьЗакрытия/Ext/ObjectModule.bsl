
Процедура ОбработкаПроведения(Отказ, Режим)
	//{{__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!

	// регистр ГрафикЗакрытияПериода
	Движения.ГрафикЗакрытияПериода.Записывать = Истина;
	Движения.ГрафикЗакрытияПериода.Очистить();
	Для Каждого ТекСтрокаКалендарь Из Календарь Цикл
		Движение = Движения.ГрафикЗакрытияПериода.Добавить();
		Движение.Период = НачалоГода(Дата);
		Движение.Месяц = ТекСтрокаКалендарь.Месяц;
		Движение.ДатаТочкиОтсечения = ТекСтрокаКалендарь.ДатаТочкиОтсечения;
		Движение.ДатаЗакрытияПериода = ТекСтрокаКалендарь.ДатаЗакрытияПериода;
	КонецЦикла;

	//}}__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
КонецПроцедуры
