
&НаКлиенте
Процедура Ок(Команда)
	Если Подразделение.Пустая() Тогда
		Предупреждение("Не выбрано подразделение");
		Возврат;
	КонецЕсли;
	Если Должность.Пустая() Тогда
		Предупреждение("Не выбрана должность");
		Возврат;
	КонецЕсли;
	Закрыть(Новый Структура("Должность, Подразделение", Должность, Подразделение));
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	Закрыть();
КонецПроцедуры
