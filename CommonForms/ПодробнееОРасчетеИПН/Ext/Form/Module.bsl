
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АдресХранилищаВычетыИПН") И
		ЭтоАдресВременногоХранилища(Параметры.АдресХранилищаВычетыИПН) Тогда	
		ТаблицаВычетыИПН 		= ПолучитьИзВременногоХранилища(Параметры.АдресХранилищаВычетыИПН);
		
		ЗначениеВРеквизитФормы(ТаблицаВычетыИПН,"ВычетыИПН");
	КонецЕсли;	
	
	Если Параметры.Свойство("МесяцНалоговогоПериода") Тогда
		МесяцНалоговогоПериода = Параметры.МесяцНалоговогоПериода;
	КонецЕсли;	
	
	Если Параметры.Свойство("ФизЛицо") Тогда
		ФизЛицо = Параметры.ФизЛицо;
	КонецЕсли;	
	
	Заголовок = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Подробнее о расчете вычетов ИПН %1: %2'")
												, ?(ТипЗнч(ФизЛицо) = Тип("СправочникСсылка.ФизическиеЛица"),НСтр("ru = 'физического лица'"),НСтр("ru = 'контрагента'")), ФизЛицо);
	
КонецПроцедуры

