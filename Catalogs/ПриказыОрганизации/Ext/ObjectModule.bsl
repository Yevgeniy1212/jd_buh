
Процедура ПриЗаписи(Отказ)
	Если ОсновнойПриказ Тогда
		Запрос = Новый Запрос("ВЫБРАТЬ Справочник.ПриказыОрганизации.Ссылка ГДЕ Ссылка <> &Ссылка И ОсновнойПриказ");
		Запрос.УстановитьПараметр("Ссылка", Ссылка);
		Выб = Запрос.Выполнить().Выбрать();
		Пока Выб.Следующий() Цикл
			Сообщить("Снят флаг основного приказа с """+СокрЛП(Выб.Ссылка)+"""");
			Об = Выб.Ссылка.ПолучитьОбъект();
			Об.ОсновнойПриказ = Ложь;
			Об.Записать();
		КонецЦикла;
	КонецЕсли;
КонецПроцедуры
